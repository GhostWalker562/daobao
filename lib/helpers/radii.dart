import 'package:flutter/material.dart';

class Radii {
  static final sr = BorderRadius.circular(2);
  static final mr = BorderRadius.circular(4);
  static final lr = BorderRadius.circular(8);
  static final xlr = BorderRadius.circular(16);
  static final xxlr = BorderRadius.circular(32);
}
